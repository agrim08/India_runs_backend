# Offline ranking dependencies
# These are the ONLY packages needed to run rank.py
# No LLM API SDKs, no cloud dependencies.

sentence-transformers>=2.2.2,<3.0.0
numpy>=1.24.0,<2.0.0
pydantic>=2.0.0,<3.0.0
torch>=2.0.0,<3.0.0
transformers>=4.30.0
huggingface_hub[hf_xet]>=0.20.0
